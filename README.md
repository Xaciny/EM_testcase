````md
# Effective Mobile — DevOps Test Task

Тестовое задание: развернуть простое веб-приложение, доступное пользователю через **nginx**, запущенные в **Docker-контейнерах**.

Backend отвечает на запрос `GET /` текстом:

```text
Hello from Effective Mobile!
````

---

## 📦 Структура проекта

```text
.
├── backend/
│   ├── Dockerfile
│   └── app.py
├── nginx/
│   └── nginx.conf
├── docker-compose.yml
├── .env
└── README.md
```

---

## 🛠 Используемые технологии

* Docker / Docker Compose
* Nginx (reverse proxy)
* Python 3 (минимальный HTTP backend)

---

## Запуск проекта

1. Клонировать репозиторий и перейти в папку проекта:

```bash
git clone git@github.com:Xaciny/EM_testcase.git
cd EM_testcase
```

2. (Опционально) Проверить/создать `.env`

Пример `.env`:

```env
NGINX_IMAGE=nginx:1.27-alpine
NGINX_PORT=80
PROJECT_NET=effective_app_net
```

3. Запустить сервисы:

```bash
docker compose up --build -d
```

4. Проверить, что контейнеры поднялись:

```bash
docker compose ps
```

---

## ✅ Проверка работоспособности

Основная проверка по ТЗ:

```bash
curl http://localhost
```

Ожидаемый ответ:

```text
Hello from Effective Mobile!
```

---

## 🧩 Как работает схема (nginx → backend)

Снаружи доступен только **nginx** на порту **80**.
Backend слушает порт **8080**, но **не публикуется наружу** — доступен только внутри docker-сети.

### ASCII схема

```text
Client (curl/browser)
        |
        v
   http://localhost:80
        |
        v
+---------------------+
|  nginx container    |
|  listen: 80         |
|  reverse proxy      |
+----------+----------+
           |
           v
+---------------------+
|  backend container  |
|  listen: 8080       |
|  only docker-net    |
+---------------------+
```

---

## 🌐 Networking

* nginx проксирует запросы в backend по имени сервиса `backend` внутри docker-сети
* наружу проброшен только порт `80` nginx
* backend не имеет `ports:` в compose, поэтому не доступен с хоста напрямую

---

## 🔒 Проверка требований по сети

Backend не должен быть доступен с хоста — это ожидаемое поведение:

```bash
curl http://localhost:8080
```

Команда должна завершиться ошибкой (порт не опубликован).

---

## 🧾 Полезные команды

### Остановить проект

```bash
docker compose down
```